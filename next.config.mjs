/** @type {import('next').NextConfig} */
const nextConfig = {
  // devIndicators: false,
  // typescript: {
  //   ignoreBuildErrors: true,
  // },
  images: {
    unoptimized: true,
  },
  output: 'export',   
}

export default nextConfig
